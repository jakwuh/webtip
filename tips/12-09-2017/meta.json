{
    "description": "Given a set of distinct integers S, return all possible subsets. Elements in a subset must be in non-descending order. The solution set must not contain duplicate subsets. Also, subsets should be sorted in ascending ( lexicographic ) order."
}
